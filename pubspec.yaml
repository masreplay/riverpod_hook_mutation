name: flutter_async_hook
description: "Flutter hook for future async operations."
version: 0.0.1
homepage: "https://github.com/masreplay/flutter_async_hook"

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_hooks: ^0.20.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
